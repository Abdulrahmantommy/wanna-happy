from binarytree import build
nodes = [1, 2, 15, 1, 11, None]
binary_tree = build(nodes)


print('Binary tree from list :\n',
      binary_tree)

print('\n',
      binary_tree.values)

for i in nodes:
    if nodes == 791:
        nodes += ([555, ["*", [1], [2]], [15], [1, [11]]])
    elif nodes == 63:
         nodes += (["*", ["*", [1], [2]], [15], [1, [11]]])